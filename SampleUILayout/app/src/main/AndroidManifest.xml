<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omegasupreme.sampleuilayout">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="@string/app_name"
            android:name=".RecordDataManualActivity"/>
        <activity
            android:label="@string/app_name"
            android:name=".ManualDataFrom"/>
        <activity
            android:label="@string/app_name"
            android:name=".DeviceList"/>
        <activity
            android:label="@string/app_name"
            android:name=".RecordDevice"/>
        <activity
            android:label="@string/app_name"
            android:name=".EnvData"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="igjoiprjoprejpjeropgjero65tr6g" />
    </application>


</manifest>
